
\usepackage{wrapfig}
\usepackage{wasysym}
\usepackage{color}
\definecolor{light-gray}{gray}{.85} 
\usepackage{xcolor}
\usepackage{subfiles}
\usepackage{subfig}
\usepackage{graphicx}
\usepackage{fontspec}
\usepackage{hyperref}
\hypersetup{% 
    bookmarks=true,         % show bookmarks bar?
    unicode=true,          % non-Latin characters in Acrobat’s bookmarks
    pdftoolbar=true,        % show Acrobat’s toolbar?
    pdfmenubar=true,        % show Acrobat’s menu?
    pdffitwindow=false,     % window fit to page when opened
    pdfstartview={FitH},    % fits the width of the page to the window
    pdftitle={بَرْمَجَة وَ تَصْمِيمْ نظَام تَشْغِيلْ},    % title
    pdfauthor={أحمد عصام عبد الرحيم أحمد},     % author
    pdfsubject={نظام تشغيل إقرأ},   % subject of the document
    pdfcreator={أحمد عصام عبد الرحيم أحمد},   % creator of the document
    pdfproducer={أحمد عصام عبد الرحيم أحمد}, % producer of the document
    pdfkeywords={برمجة نظام تشغيل}, % list of keywords
    pdfnewwindow=true,      % links in new window
    pdfborder = {0 0 0} 
} 

\usepackage[all]{hypcap}
\urlstyle{same}

\usepackage{comment}
\usepackage{todonotes}
%\usepackage[helvetica]{quotchap}

%\input{listingsconf.tex}
\usepackage{listings}
\usepackage{caption}
\DeclareCaptionFont{white}{\color{white}}
\DeclareCaptionFormat{listing}{\colorbox{gray}{\parbox{\textwidth}{#1#2#3}}}
\captionsetup[lstlisting]{format=listing,labelfont=white,textfont=white}


\renewcommand*\lstlistlistingname{الأمثلة التوضيحية}
\renewcommand*\lstlistingname{\en{Example}}


%\usepackage[Lenny]{fncychap}

\providecommand{\newXeTeXintercharclass}[1]{\edef #1\relax}					% perevent errors in windows(MikeTex)
\usepackage{polyglossia}


\pagestyle{headings}

\setmainlanguage{arabic}
\setotherlanguage{english}

\setmainfont[Script=Arabic,Scale=1.4,Mapping=arabicdigits]{Traditional Arabic}

\newcommand{\en}[1]{%
{\fontspec[Scale=1.0,Mapping=englishdigits]{Calibri}%Traditional Arabic
{\beginL #1}{\endL}}}

\newcommand{\cmd}[1]{%
{\fontspec[Scale=1.0,Mapping=englishdigits]{Courier New}%Consolas
{\beginL #1}{\endL}}}



\setlength{\parindent}{0in}


\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}




\title{
بَرْمَجَة وَ تَصْمِيمْ نظَام تَشْغِيلْ\\ "نظام إقرأ"
}

\author{
أحمد بن عصام عبد الرحيم أحمد
}

\date{\today}

%\makeatletter
 %\AtBeginDocument{\let\@arabic\default@arabic}
%\makeatother

\makeatletter
\@ifpackageloaded{caption}{\input{caption-xetex-bidi.def}}{}
\makeatother

\makeatletter
\renewcommand\part{%
  \if@openright
    \cleardoublepage
  \else
    \clearpage
  \fi
  \thispagestyle{empty}%
  \if@twocolumn
    \onecolumn
    \@tempswatrue
  \else
    \@tempswafalse
  \fi
  \null\vfil
  \secdef\@part\@spart}
\makeatother
